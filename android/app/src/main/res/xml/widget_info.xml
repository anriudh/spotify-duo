<?xml version="1.0" encoding="utf-8"?>
<!-- updatePeriodMillis is 0 on purpose: the system's automatic update has a
     30-minute floor, so refreshes are driven by our own alarm instead.
     minResize* is the resize floor; without it the floor is minWidth/minHeight,
     which on a phone with ~90dp cells leaves only two legal widths. The
     floor sits well under one cell so a single row is always offered; the
     card switches to its compact layout when it gets that short. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:initialLayout="@layout/widget_card"
    android:minHeight="110dp"
    android:minResizeHeight="60dp"
    android:minResizeWidth="150dp"
    android:minWidth="250dp"
    android:previewLayout="@layout/widget_card"
    android:resizeMode="horizontal|vertical"
    android:targetCellHeight="2"
    android:targetCellWidth="4"
    android:updatePeriodMillis="0"
    android:widgetCategory="home_screen" />
